CREATE TABLE SUPPLIER (
    supplier_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    company_name VARCHAR(255),
    website VARCHAR(255),
    location VARCHAR(255),
    nature_of_business VARCHAR(50),
    manufacturing_processes VARCHAR(50)
);
